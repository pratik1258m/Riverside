{
  "name": "the-river-farm",
  "version": "1.0.0",
  "description": "The Riverside Farm Website",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html style.css script.js robots.txt sitemap.xml dist/ && cp -r assets dist/ && tailwindcss -i ./input.css -o ./dist/output.css --minify",
    "build:css": "tailwindcss -i ./input.css -o ./output.css",
    "watch:css": "tailwindcss -i ./input.css -o ./output.css --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
